# Plaintext template — copy to production.enc.yaml, fill values,
# then `sops -e -i production.enc.yaml`.
#
# `sops --decrypt --output-type dotenv` flattens this YAML to KEY=value lines
# suitable for systemd EnvironmentFile.

DATABASE_URL: postgres://app_write:REPLACE@10.0.1.10:6432/vicionpower

BETTER_AUTH_SECRET: REPLACE_with_openssl_rand_base64_32
BETTER_AUTH_URL: https://app.vicionpower.com
BETTER_AUTH_TRUSTED_ORIGINS: https://app.vicionpower.com

REDIS_URL: redis://default:REPLACE@10.0.1.30:6379

RESEND_API_KEY: re_REPLACE

GOOGLE_CLIENT_ID: REPLACE.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET: REPLACE

PII_ENCRYPTION_KEY: REPLACE_with_openssl_rand_base64_32

NODE_ENV: production
PORT: "3000"
PGBOUNCER: "true"
